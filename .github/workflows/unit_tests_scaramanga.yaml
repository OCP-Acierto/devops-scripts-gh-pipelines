name: Execute UnitTests
on:
  workflow_call:
jobs:
  job:
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Execute UnitTests
        run: |
          npm install
          npm run test
